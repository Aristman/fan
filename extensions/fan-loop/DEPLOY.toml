# DEPLOY.toml — FAN Package Deployment Manifest
# Schema version: 1

[package]
name = "fan-loop"
type = "extension"

include = [
    "index.ts",
    "loop-controller.ts",
    "types.ts",
    "config.json",
    "package.json",
    "README.md",
]

exclude = [
    "node_modules/",
    "*.test.ts",
    "*.spec.ts",
    "vitest.config.*",
    ".env",
    ".env.*",
    "dist/",
    "*.lock",
    "package-lock.json",
    "bun.lock",
    ".git/",
    ".DS_Store",
]

preserve = [
    "config.json",
]
