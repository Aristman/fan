# DEPLOY.toml — FAN Package Deployment Manifest
# Schema version: 1

[package]
name = "fan-soul"
type = "extension"

include = [
    "index.ts",
    "drafts.ts",
    "extractor.ts",
    "soul-files.ts",
    "types.ts",
    "package.json",
    "README.md",
]

exclude = [
    "node_modules/",
    "*.test.ts",
    "*.spec.ts",
    "vitest.config.*",
    ".env",
    ".env.*",
    "dist/",
    "*.lock",
    "package-lock.json",
    "bun.lock",
    ".git/",
    ".DS_Store",
]

preserve = []
