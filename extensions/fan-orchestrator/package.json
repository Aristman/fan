{
	"name": "fan-orchestrator",
	"version": "7.6.1",
	"description": "FAN orchestrator v7.6.0 — Parallel read-only agents (v7.4.0): toWorkerType now uses agent.readOnly flag instead of hardcoded whitelist. All readOnly agents (code-research, explore, plan, verify) run in parallel. Write agents (implement, bug-fix, tests-impl, docs-impl) remain serialized in exclusive implement slot.",
	"type": "module",
	"main": "./orchestrator-extension.js",
	"types": "./orchestrator-extension.d.ts",
	"fan": {
		"name": "fan-orchestrator",
		"type": "extension",
		"displayName": "FAN Orchestrator v7.6.0",
		"description": "FAN orchestrator v7.6.0 — Parallel read-only agents: toWorkerType uses readOnly flag. All readOnly agents get independent parallel slot pools. Write agents remain exclusive. Built on 7.4.0 Pipeline Mode.",
		"version": "7.6.1",
		"main": "orchestrator-extension.js",
		"entry": "src/orchestrator-extension.ts",
		"author": "FAN Team",
		"tags": [
			"orchestrator",
			"multi-agent",
			"workers",
			"tasks",
			"planning",
			"delegate",
			"assess",
			"complexity"
		],
		"bundled": true
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.0"
	}
}
