{
	"name": "fan-orchestrator",
	"version": "7.7.0",
	"description": "FAN orchestrator v7.7.0 — MCP integration: Phase 1+2+3 (coordinator MCP, worker proxy, profile filtering, /mcp status, OAuth, auto-restart, structuredContent, progress, logging) + BUG-1..10 critical fixes + lint cleanup",
	"type": "module",
	"main": "./orchestrator-extension.js",
	"types": "./orchestrator-extension.d.ts",
	"fan": {
		"name": "fan-orchestrator",
		"type": "extension",
		"displayName": "FAN Orchestrator v7.7.0",
		"description": "FAN orchestrator v7.7.0 — MCP integration: Phase 1+2+3 (coordinator MCP, worker proxy, profile filtering, /mcp status, OAuth, auto-restart, structuredContent, progress, logging) + BUG-1..10 critical fixes + lint cleanup",
		"version": "7.7.0",
		"main": "orchestrator-extension.js",
		"entry": "src/orchestrator-extension.ts",
		"author": "FAN Team",
		"tags": [
			"orchestrator",
			"multi-agent",
			"workers",
			"tasks",
			"planning",
			"delegate",
			"assess",
			"complexity"
		],
		"bundled": true
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.0"
	}
}
