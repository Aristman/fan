{
	"name": "fan-orchestrator",
	"version": "7.7.1",
	"description": "FAN orchestrator v7.7.1 — Qwen model config, clear OFF status, E2E test cleanup. MCP integration: coordinator MCP, worker proxy, profile filtering, /mcp status, OAuth, auto-restart, structuredContent, progress, logging.",
	"type": "module",
	"main": "./orchestrator-extension.js",
	"types": "./orchestrator-extension.d.ts",
	"fan": {
		"name": "fan-orchestrator",
		"type": "extension",
		"displayName": "FAN Orchestrator v7.7.1",
		"description": "FAN orchestrator v7.7.1 — Qwen model config, clear OFF status, E2E test cleanup. MCP integration: coordinator MCP, worker proxy, profile filtering, /mcp status, OAuth, auto-restart, structuredContent, progress, logging.",
		"version": "7.7.1",
		"main": "orchestrator-extension.js",
		"entry": "src/orchestrator-extension.ts",
		"author": "FAN Team",
		"tags": [
			"orchestrator",
			"multi-agent",
			"workers",
			"tasks",
			"planning",
			"delegate",
			"assess",
			"complexity"
		],
		"bundled": true
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.0"
	}
}
