{
	"name": "fan-orchestrator",
	"version": "7.2.0",
	"description": "FAN orchestrator v7.2.0 — worker reliability fix: tool-only response fallback, idle-poll detection, prevents Plan worker from returning empty output", 
	"type": "module",
	"main": "./orchestrator-extension.js",
	"types": "./orchestrator-extension.d.ts",
	"fan": {
		"name": "fan-orchestrator",
		"type": "extension",
		"displayName": "FAN Orchestrator v7.2.0",
		"description": "FAN orchestrator v7.2.0 — worker reliability fix: tool-only response fallback, idle-poll detection, prevents Plan worker from returning empty output",
		"version": "7.2.0",
		"main": "orchestrator-extension.js",
		"entry": "src/orchestrator-extension.ts",
		"author": "FAN Team",
		"tags": [
			"orchestrator",
			"multi-agent",
			"workers",
			"tasks",
			"planning",
			"delegate",
			"assess",
			"complexity"
		],
		"bundled": true
	},
	"dependencies": {
		"@sinclair/typebox": "^0.34.0"
	}
}
