{
  "cloud": { "defaultModel": "", "defaultProvider": "", "models": {}, "providers": {} },
  "local": { "defaultModel": "", "defaultProvider": "ollama", "models": {}, "providers": {} },
  "providerMode": "cloud",
  "maxWorkers": 10,
  "parallelWorkers": 3,
  "maxRetries": 2,
  "stallTimeout": 300000,
  "dangerousCommands": ["rm -rf /", "rm -rf .", "git push --force", "npm publish", "DROP TABLE"]
}
