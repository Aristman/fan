// Default ROADMAP.md template for fan-mission init.

export const ROADMAP_MD = `# Roadmap

<!-- Keep roadmap items atomic: one focused change per item, completable and verifiable in a single ~20–30 minute iteration. Split oversized areas into several items. -->

- [ ] Bootstrap mission: {{slug}}
`;
