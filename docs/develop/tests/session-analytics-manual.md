# Ручное тестирование: fan-session-analytics (v1.3.0)

> Дата: 2026-08-02 · Этапы A+B+C · Перед прогоном: обновить установленную копию расширения и перезапустить FAN

## 0. Подготовка

```powershell
# Обновить установленную копию (config.json и state.json сохранятся — их нет в исходниках)
xcopy /E /I /Y extensions\fan-session-analytics "%USERPROFILE%\.fan\agent\extensions\fan-session-analytics"
```

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 0.1 | Запустить FAN, ввести `/session-analytics` и нажать Tab | Автодополнение: `last`, `dir`, `init`, `config`, `mark-golden`, `unmark-golden`, `golden` | ☐ |
| 0.2 | `/session-analytics config` | Таблица эффективной конфигурации с пометками источника (📋 defaults / 📄 config.json / 📁 project) | ☐ |

## 1. Мастер init

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 1.1 | `/session-analytics init` | Мастер: авто-анализ → еженедельный пакет → порог overhead → тяжёлые скилы → каталог отчётов → модель судьи | ☐ |
| 1.2 | Шаг «тяжёлые скилы» | Список установленных скилов с ✔/☐; Enter переключает, **курсор остаётся на элементе** (не прыгает наверх); «Готово» завершает | ☐ |
| 1.3 | Шаг «модель судьи» | Два шага: провайдер → модель из доступных; «Не настраивать» пропускает | ☐ |
| 1.4 | Финал | `config.json` создан в каталоге расширения; `/session-analytics config` показывает 📄 config.json | ☐ |
| 1.5 | Повторный init → «Не настраивать» на шаге судьи | Прежние `judge.provider/model` **сохранены** (регрессия M1) | ☐ |
| 1.6 | Esc на любом шаге | Отмена без записи; прежний config не изменился | ☐ |

## 2. Ручной анализ (mode: metrics)

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 2.1 | `/session-analytics last` | Сводка: балл, находки по severity, путь к отчёту. Без вызова судьи (мгновенно) | ☐ |
| 2.2 | `/session-analytics dir` | Пакет по всем сессиям каталога: карточки ✅/⚠️/❌, строка «Паттернов найдено: N» | ☐ |
| 2.3 | Открыть отчёт из 2.1 | Структура: заголовок → балл → метрики D1–D13 → находки → рекомендации → сводка инструментов. Всё на русском. Версия в футере = 1.3.0 | ☐ |
| 2.4 | `/session-analytics <несуществующий путь>` | Понятное сообщение об ошибке, без стектрейса | ☐ |
| 2.5 | Сводный отчёт dir (`.fan/reports/session-analytics/`) | Секция «Рекомендации по синтезу»: паттерны с частотой, примерами сессий, пометкой BR4 | ☐ |

## 3. Судья (mode: full)

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 3.1 | Попросить агента: `session_analyze target:"last" mode:"full"` | Прогон с судьёй: секция «Оценки судьи» (рубрики 0–3 с обоснованиями и шагами), «Рекомендации судьи», «Расход на судью» | ☐ |
| 3.2 | Combined score | В сводке: det×0.6 + судья×0.4; обоснования содержательные (не «Средний балл по N батчам») | ☐ |
| 3.3 | Сессия без оркестратора (без delegate_task) | Рубрики «Качество декомпозиции» и «Качество оркестрации» = n/a (не 0) | ☐ |
| 3.4 | Временно задать в init несуществующую модель судьи → full | Деградация: fallback на дешёвую доступную или «Судья недоступен», deterministic-only. НЕ падение | ☐ |

## 4. Золотые траектории (F12)

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 4.1 | `/session-analytics mark-golden last эталон-исследования` | Подтверждение; `/session-analytics golden` показывает запись | ☐ |
| 4.2 | Повторный mark-golden той же сессии с другим label | Одна запись, label обновлён (нет дублей) | ☐ |
| 4.3 | Full-анализ другой сессии того же каталога | Секция «Сравнение с эталоном»: alignment 0–3, таблица отклонений, verdict | ☐ |
| 4.4 | Full-анализ самой эталонной сессии | Self-comparison НЕ происходит (эталон исключён) | ☐ |
| 4.5 | `/session-analytics unmark-golden <sessionId>` | Запись удалена; `golden` пуст | ☐ |

## 5. Авто-режим F9 (shutdown)

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 5.1 | Включить autoAnalyze в init (или config.json) | — | ☐ |
| 5.2 | Поработать в сессии, завершить FAN | Shutdown не зависает (анализ быстрый, 1–3 сек) | ☐ |
| 5.3 | Запустить FAN заново | Однострочник: «Аналитика прошлой сессии: 87/100, петель: 0, ошибок: 2…» | ☐ |
| 5.4 | Перезапустить FAN ещё раз | Однострочник НЕ повторяется (показ один раз) | ☐ |
| 5.5 | Сессия с вызовом session_analyze → завершить | Авто-анализ пропущен (BR3, нет сводки) | ☐ |

## 6. Еженедельный пакет F10

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 6.1 | Включить weeklyBatch; в `state.json` сдвинуть `lastBatchRun` на −8 дней | При старте сессии: confirm «Запустить еженедельный анализ? (~N файлов)» | ☐ |
| 6.2 | Ответить «Нет» | Отложено на день; при перезапуске сегодня больше не спрашивает | ☐ |
| 6.3 | Ответить «Да» | Анализ в фоне (старт НЕ блокируется!); по завершении notify с путём к `weekly_<date>.md` | ☐ |
| 6.4 | Открыть weekly-отчёт | Таблица сессий + тренд-блок (↑↓ против прошлого периода или пометка «первый запуск») + «Рекомендации по синтезу» | ☐ |
| 6.5 | Второй weekly через неделю (или повторить 6.1) | Тренд-блок с реальными Δ против прошлого прогона | ☐ |
| 6.6 | weeklyBatch.silent: true | Без confirm — молча в фоне | ☐ |

## 7. Дашборд (F11)

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 7.1 | `fan --web` из корня проекта | Дашборд открывается | ☐ |
| 7.2 | Sidebar → «Аналитика» | Список отчётов: карточки (имя, бейдж сессия/неделя, дата, размер) | ☐ |
| 7.3 | Клик по отчёту | Detail: markdown в моноширинном виде; «Назад к списку» работает | ☐ |
| 7.4 | Сгенерировать новый отчёт (любой анализ) → подождать ≤30 сек | Отчёт появился в списке без перезагрузки (polling) | ☐ |
| 7.5 | Пустой каталог отчётов (временно переименовать) | Empty state с подсказкой запустить `/session-analytics` | ☐ |
| 7.6 | curl `GET /api/analytics/reports/..%2F..%2Fstate.json` с токеном | 400 (path traversal отклонён) | ☐ |

## 8. Регрессия и граничные случаи

| # | Шаг | Ожидаемый результат | Статус |
|---|-----|--------------------|--------|
| 8.1 | Прогон на известной сессии d4c01817 (7.6 МБ) | 78 tool calls, D12: 10 medium (implement→research), p95 ~2.3 мин | ☐ |
| 8.2 | Сессия с verify после баг-фикса | D12 НЕ флагает verify (регрессия ложных срабатываний) | ☐ |
| 8.3 | Битый jsonl (обрезать файл вручную, анализ по пути) | Отчёт с флагом truncated, без падения | ☐ |
| 8.4 | `bun run scripts/smoke.ts` в каталоге расширения | 10/10 PASS, файлы сессий не изменились (mtime/size) | ☐ |
| 8.5 | Все 4 тест-набора | smoke 10 · verify-judge 65 · verify-auto 45 · verify-golden-patterns 128 | ☐ |

## Правила фиксации результатов

- Каждый FAIL → issue с шагом, ожиданием, фактом, скриншотом/логом
- Калибровочные замечания (ложные срабатывания детекторов) — отдельным списком, это вход для следующей итерации, не обязательно баги

---

*Создано вручную по аналогии с dashboard-phase6.md · Автотесты: 248 (расширение) + 51 (api-gateway) + 23 (dashboard)*
