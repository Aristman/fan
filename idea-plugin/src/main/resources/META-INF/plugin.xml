<idea-plugin>
    <id>fan.idea.plugin</id>
    <name>FAN Agent</name>
    <version>1.0.0</version>
    <vendor>Filin Agent Next</vendor>
    <description><![CDATA[
        AI coding agent integration powered by FAN Runtime.<br/>
        Connect to your local FAN Server for AI-assisted coding directly in your IDE.
    ]]></description>

    <depends>com.intellij.modules.platform</depends>
    <depends>com.intellij.modules.lang</depends>

    <extensions defaultExtensionNs="com.intellij">
        <toolWindow id="FAN Agent"
                    factoryClass="fan.idea.toolwindow.FanToolWindowFactory"
                    anchor="right"
                    icon="fan.idea.icons.FanIcon"/>

        <applicationConfigurable
            instance="fan.idea.settings.FanPluginConfigurable"
            id="fan.settings"
            displayName="FAN Agent"/>

        <notificationGroup
            id="fan.notifications"
            displayType="BALLOON"
            key="fan.notification.group.name"/>

        <statusBarWidgetFactory implementation="fan.idea.ui.status.FanStatusWidgetFactory" id="FAN_ConnectionStatus"/>

    </extensions>

    <actions>
        <action id="fan.askFan"
                class="fan.idea.editor.AskFanAction"
                text="Ask FAN"
                description="Send selected code to FAN Agent">
            <add-to-group group-id="EditorPopupMenu" anchor="last"/>
            <keyboard-shortcut first-keystroke="alt F" keymap="$default"/>
        </action>

        <action id="fan.openSettings"
                class="fan.idea.actions.OpenSettingsAction"
                text="FAN Settings"
                description="Open FAN Agent settings"/>
    </actions>
</idea-plugin>
